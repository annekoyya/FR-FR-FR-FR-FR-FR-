fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
# the rest (numpy, opencv, pillow, qrcode, torch, torchvision, xgboost,
# scikit-learn, pyzbar) come from your thesis project's own requirements.txt --
# install that FIRST, in the same venv.
